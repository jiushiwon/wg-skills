# Vue Select Skill

Vue3 下拉选择器组件，纯 CSS+JS 实现。

## 功能

- 单选/多选
- 支持搜索
- 支持远程搜索
- 支持分组
- 禁用/清空

## 使用

```vue
<BaseSelect v-model="value" :options="options" placeholder="请选择" />
```

## 触发词

"Vue 选择器"、"vue-select"、"下拉组件"
