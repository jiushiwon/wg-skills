# Vue Input Skill

Vue3 输入框组件，基于原生 input 封装。

## 功能

- 三种尺寸（small/default/large）
- 前缀/后缀图标
- 可清空
- 禁用/只读

## 使用

```vue
<BaseInput v-model="value" placeholder="请输入" />
```

## 触发词

"Vue 输入框"、"vue-input"、"输入框组件"
